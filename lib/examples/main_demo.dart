// Design System Demo Entry Point
//
// Run this file directly to view the design system demo:
//   flutter run -t lib/examples/main_demo.dart -d chrome --web-port=8081
//
// This keeps the demo page accessible even when the main app is running.

import 'package:flutter/material.dart';
import 'design_system_demo.dart';

void main() {
  runApp(const DesignSystemDemo());
}
